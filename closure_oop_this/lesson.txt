замыкание

const name = 'John'

function sayHi () {
    console.log('Hi ${name}')
}

sayHi()

Функция которая имеет доступ ко внешним областям видимости и может менять то что в них

ООП - парадигма которая диктует как писать программу, как должен выглядеть код

This работает как ссылка на объект

контекст - this (одно и тоже) это обьект в рамках которого вызывается функция

this = объекту перед которым стоит точка

метод - свойство которое можно вызвать\функция в которой записан